# ── 部署模式 ──
# 设置 AI_STUDIO_TOKEN 后，扫描件 PDF / 图片走 AI Studio 托管 API，
# 无需自建 vLLM。留空则走本地 vLLM 模式。
AI_STUDIO_TOKEN=

# 提示：其余配置（端口、VLLM_SERVER_URL、AI_STUDIO_MODEL、JWT_SECRET 等）
# 直接写在 docker-compose.yaml 中，需要覆盖时修改对应行即可
# （docker-compose v1 不支持 "${VAR:-default}" 插值，故不在此提供）。
